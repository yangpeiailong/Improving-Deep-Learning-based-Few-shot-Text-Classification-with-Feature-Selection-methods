name: fs-lrtc-py312
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - PyYAML>=6.0.2,<7
      - numpy==1.26.4
      - scipy==1.15.2
      - pandas==2.2.3
      - scikit-learn==1.6.1
      - transformers==4.49.0
      - tokenizers==0.21.0
      - huggingface-hub==0.29.1
      - safetensors==0.5.3
      - tqdm==4.67.1
      - psutil==7.0.0
      - regex==2024.11.6
      - requests==2.32.3
      - fasttext-wheel==0.9.2
# Install the appropriate PyTorch build separately; the released Windows/CUDA
# environment used requirements-torch-cu124.txt.
